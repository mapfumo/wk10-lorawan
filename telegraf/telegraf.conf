# Telegraf Configuration for LoRaWAN Gateway MQTT → InfluxDB
# Subscribes to RAK7268V2 gateway and writes decoded sensor data to InfluxDB

[agent]
  interval = "10s"
  round_interval = true
  metric_batch_size = 1000
  metric_buffer_limit = 10000
  collection_jitter = "0s"
  flush_interval = "10s"
  flush_jitter = "0s"
  precision = "0s"
  hostname = "lorawan-telegraf"
  omit_hostname = false

# Output to InfluxDB 2.x (Week 7 infrastructure)
[[outputs.influxdb_v2]]
  urls = ["http://wk7-influxdb:8086"]
  token = "my-super-secret-auth-token"
  organization = "my-org"
  bucket = "lorawan"

# Input from RAK7268V2 Gateway MQTT Broker
[[inputs.mqtt_consumer]]
  servers = ["tcp://10.10.10.254:1883"]
  topics = ["application/TOT/device/+/rx"]
  qos = 0
  connection_timeout = "30s"
  client_id = "telegraf-lorawan"
  data_format = "json_v2"

  [[inputs.mqtt_consumer.json_v2]]
    measurement_name = "lorawan_uplink"

    # Extract device EUI from topic
    [[inputs.mqtt_consumer.json_v2.tag]]
      path = "devEUI"
      rename = "dev_eui"

    # Extract radio metadata
    [[inputs.mqtt_consumer.json_v2.field]]
      path = "rxInfo.0.rssi"
      rename = "rssi"
      type = "int"

    [[inputs.mqtt_consumer.json_v2.field]]
      path = "rxInfo.0.loRaSNR"
      rename = "snr"
      type = "float"

    [[inputs.mqtt_consumer.json_v2.field]]
      path = "fCnt"
      rename = "frame_count"
      type = "int"

    [[inputs.mqtt_consumer.json_v2.field]]
      path = "fPort"
      rename = "port"
      type = "int"

    # Store raw payload for processing
    [[inputs.mqtt_consumer.json_v2.field]]
      path = "data"
      rename = "payload_base64"
      type = "string"

# Processor to decode base64 payload and extract sensor values
# Note: Telegraf's built-in processors can't decode custom binary payloads
# We'll use an exec processor with a Python script for decoding
[[processors.execd]]
  command = ["/usr/bin/python3", "/etc/telegraf/decode_lorawan.py"]
  signal = "none"
